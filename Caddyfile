# This safe bootstrap stays on HTTP until the updater renders host settings.
:80 {
    @novajs_health path /__novajs_health
    respond @novajs_health 204

    encode gzip
    reverse_proxy novajs:8200
}
