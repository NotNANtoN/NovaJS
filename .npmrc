# @pixi/particle-emitter@5 declares a peer of @pixi/core@7.4.x while we pin
# 7.2.4; strict peer resolution makes a plain `npm install` fail with ERESOLVE.
# The lockfile tree is known-good, so accept legacy peer resolution.
legacy-peer-deps=true
