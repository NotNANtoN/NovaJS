[package]
name = "nova_wasm"
version = "0.1.0"
edition = "2021"
description = "Rust/WebAssembly numeric hot paths for NovaJS"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

