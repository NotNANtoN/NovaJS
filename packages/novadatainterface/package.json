{
  "name": "novadatainterface",
  "version": "0.1.6",
  "description": "An interface for game data for NovaJS",
  "license": "MIT",
  "scripts": {
    "build-defaults": "ts-node scripts/pack_png.ts defaults/default_pict.png src/default_pict.ts && ts-node scripts/pack_png.ts defaults/default_rled.png src/default_rled.ts",
    "build": "npm run build-defaults && tsc",
    "test": "jasmine --config=jasmine.json"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@evn/tsconfig": "0.1.0"
  }
}
